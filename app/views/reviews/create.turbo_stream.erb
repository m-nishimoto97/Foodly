<%= turbo_stream.prepend "reviews_frame",
  partial: "reviews/review_list",
  locals: { reviews: [@review] } %>
<%= turbo_stream.replace "reviews_form" do %>
  <%= render "reviews/review_form", review: @recipe.reviews.build, recipe: @recipe %>
<% end %>
