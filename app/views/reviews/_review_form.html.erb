<div class="new-review-form mt-3">
  <%= simple_form_for(review, url: recipe_reviews_path(recipe)) do |f| %>
    <%= f.input :rating %>
    <%= f.input :comment %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.submit "Add Review", class: "btn btn-cYellow w-100" %>
  <% end %>
</div>
